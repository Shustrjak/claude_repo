# UI-архитектура Banking Shell

**Banking Shell** — PET-проект: интерфейс банковского приложения и контракт данных к нему. Настоящего банковского ядра нет **намеренно**: его можно подменить или не подключать вовсе.

Этот документ — точка входа в UI-спецификацию. Он связывает шесть уровней проектирования и фиксирует границу между интерфейсом и данными.
Реализации пока нет. Кода, компонентов, API и адаптеров на этом этапе не пишем (см. [ROADMAP](../../ROADMAP.md)).

| Документ | Уровень | Что внутри |
|---|---|---|
| [SCREEN_INVENTORY.md](SCREEN_INVENTORY.md) | 1 | Реестр узлов схемы, 36 экранов, карточки экранов, зарезервированные разделы |
| [USER_FLOWS.md](USER_FLOWS.md) | 2 | Флоу A–H, предложенные состояния PST |
| [COMPONENT_INVENTORY.md](COMPONENT_INVENTORY.md) | 3 | Примитивы, семантические компоненты, матрица переиспользования |
| [SEMANTIC_COMPONENTS.md](SEMANTIC_COMPONENTS.md) | 4 | Поведение и границы логики каждого компонента |
| [SCREEN_COMPOSITION.md](SCREEN_COMPOSITION.md) | 3–4 | Из чего собран каждый экран, анализ дублирования |
| **UI_ARCHITECTURE.md** | Все | Этот документ: карта уровней, журнал решений, граница адаптера, открытые вопросы |

Источник для уровней 1–2 — схема User Flow из R38: [`refs/R38-user-flow.jpg`](../../refs/R38-user-flow.jpg).

---

## Журнал решений

Решения, принятые по открытым вопросам. В документах на них ссылаются метки `[D-xx]`.

| № | Решение | Закрывает | Что изменилось |
|---|---|---|---|
| D-01 | **Платёжная система — СБП**, регион — Россия. UPI со схемы заменяется на СБП, реквизиты банка — номер счёта и БИК | Q-14; вопрос 4 в ROADMAP | PAY-07 → SBP-01, PAY-08 → SBP-03, PAY-09 → SBP-02. PAY-05 (UPI ID) стал кандидатом, затем убран по D-06 |
| D-02 | **Главный онбординг — по R38**: регистрация → SIM → личные данные и данные карты → MPIN → OTP | Q-04; вопрос 5 в ROADMAP | Этап 1 ROADMAP перестроен под R38. Поля из R01 — кандидаты в AUTH-02 и AUTH-05 (Q-22) |
| D-03 | **Возможности устройства:** выбор и привязка SIM имитируются, биометрия — через WebAuthn, камера для QR — только по HTTPS | Q-05 | AUTH-03 и AUTH-04 работают на демо-данных |
| D-04 | **Pay, СБП и Функции СБП** — три навигационных узла одного домена платежей, а не три бизнес-раздела. PAY-01 Pay — один экран с двумя входами (нижняя навигация и меню). SBP-01 СБП — раздел с четырьмя способами перевода. SBP-02 Функции СБП стоит рядом с Settings, с SBP-01 не объединяется, статус CANDIDATE / NAVIGATION GROUP. Схема читается так: верхние ветки (поиск, профиль, выписки, уведомления, меню) — главная; ветки от Account Information и ниже — содержимое меню | Q-06, Q-09 | Флоу C, D, G, H; карточки HOME-01, HOME-04 и экранов меню |
| D-05 | **Кредитная карта, кешбэк и накопления** (ROADMAP, этапы 5–7): место в архитектуре оставлено, проектирование — отдельно | Q-20 | Зарезервированы префиксы `CREDIT-xx`, `CASHBACK-xx`, `SAVE-xx` и разделы в контракте |
| D-06 | **Узлы UPI в СБП:** перевод по UPI ID убран; перевод по реквизитам остаётся в разделе СБП; «Подключение СБП» (SBP-03) — сделать этот банк банком по умолчанию для входящих переводов СБП; при переводе по телефону пользователь выбирает банк получателя | Q-23 | Узел N45 исключён, экранов 36. В PAY-04 добавлен выбор банка, `RecipientInput(mobile)` получает список банков от адаптера |

---

## Шесть уровней

```
                    BANKING SHELL
                         │
                         ▼
   1  SCREEN MAP         36 экранов: 29 CONFIRMED, 7 CANDIDATE     ── черновик, есть вопросы
                         │
                         ▼
   2  USER FLOWS         флоу A–H, 3 предложенных состояния PST     ── черновик, есть вопросы
                         │
                         ▼
   3  COMPONENTS         20 примитивов, 3 глобальных, 24 семантических  ── черновик
                         │
                         ▼
   4  COMPONENT LOGIC    входные данные, состояния, события         ── черновик
                         │
                         ▼
   5  API CONTRACT       только перечень данных и действий          ── ЗАБЛОКИРОВАН
                         │
                         ▼
   6  ADAPTER            только граница                             ── ЗАБЛОКИРОВАН
                         │
             ┌───────────┼─────────────────────┐
             ▼           ▼                     ▼
       DemoAdapter   NullAdapter   FutureRealBankAdapter
       (JSON)        (ядра нет)    (вне scope)
```

**Уровни 5 и 6 заблокированы.** Их нельзя проектировать, пока на уровнях 1–4 есть открытые вопросы, которые меняют состав данных: Q-11, Q-12, Q-22. Ниже приведён только перечень того, что понадобится. Это не контракт.

| Уровень | Статус | Что мешает завершить |
|---|---|---|
| 1 Screen Map | Черновик | 7 кандидатов (Q-11, Q-12), расхождение числа узлов (Q-01) |
| 2 User Flows | Черновик | Тупики: Open Account (Q-02), Pay без исходящих связей (Q-11); порядок OTP (Q-07) |
| 3 Components | Черновик | Содержимое 9 экранов неизвестно (Q-11) |
| 4 Component Logic | Черновик | Длина MPIN (Q-03), MPIN при оплате (Q-18) |
| 5 API Contract | Заблокирован | Уровни 1–4 |
| 6 Adapter | Заблокирован | Уровень 5 |

---

## Слои интерфейса

```
Композиция экрана (слой C)      AUTH-01, PAY-04, …
   ├─ знает маршрут и флоу
   └─ вызывает хук фичи ─────►  хук фичи (usePayment, useAccounts, …)
        ↓ передаёт данные                 │
Семантические компоненты (слой B)         │ типы из API-контракта
   └─ знают смысл: платёж, счёт, OTP      ▼
        ↓                          Интерфейс адаптера (уровень 6)
Примитивы (слой A)                        │
   └─ только отображение          DemoAdapter | NullAdapter | …
```

### Границы логики

| Слой | Что знает | Чего не знает |
|---|---|---|
| Примитив | Как нарисовать поле, кнопку, строку | Ничего о банке |
| Семантический компонент | Смысл: сумма, получатель, статус, намерение `confirm` | На каком он экране; откуда данные |
| Композиция экрана | Какие компоненты собрать; куда вести дальше | Как устроен адаптер |
| Хук фичи | Какой метод адаптера вызвать; состояние запроса (TanStack Query) | Как это нарисовано |
| Адаптер | Откуда данные: JSON, «ничего» или реальный банк | Что есть интерфейс |

**Бизнес-правила** («хватает ли денег», «верен ли MPIN», «лимит превышен») решает **только адаптер**. Результат возвращается как статус или ошибка из контракта. Интерфейс их показывает, но не вычисляет.

Это соответствует слоям data → domain ← ui из [R60](../../refs/README.md). Но отдельный доменный слой **не вводим**, пока не появится логика, которой нет места ни в адаптере, ни в хуке фичи.

---

## Уровень 5 — что понадобится от контракта (черновик)

Выведено из экранов. Это **перечень**, а не API: здесь нет эндпоинтов, типов и форматов.

| Раздел | Данные для чтения | Действия | Экраны |
|---|---|---|---|
| Сессия и вход | Способы входа, доступна ли биометрия | Вход по MPIN, вход по биометрии, выход | AUTH-01, SET-01 |
| Регистрация | Список SIM (имитация, D-03) | Регистрация, привязка SIM, отправка данных, установка MPIN, проверка OTP, повторная отправка OTP | AUTH-02…07 |
| Счета | Счета, реквизиты, операции (последние N и за период) | — | HOME-01, ACC-01…03 |
| Открытие счёта | Типы счетов | UNKNOWN (Q-12) | ACC-07, ACC-04…06 |
| Платежи | Способы оплаты, лимиты, комиссия, найденный получатель, поставщики услуг | Проверить получателя, подготовить платёж, подтвердить платёж | PAY-01…06, PST-01, PST-02 |
| СБП | Выбран ли этот банк банком по умолчанию; банки получателя по номеру телефона | Сделать банком по умолчанию [D-06] | SBP-01, SBP-03, PAY-04 |
| Карты | Карты и их статусы | UNKNOWN (Q-15); в ROADMAP — заморозка и лимиты [R] | CARD-01 |
| Уведомления | Список, признак «прочитано» | Отметить прочитанным [A] | NOTIF-01 |
| Настройки | Текущий язык, состояние биометрии | Сменить MPIN, включить или выключить биометрию, сменить язык | SET-01…04 |
| Сервисы | Отделения (Q-12) | — | SVC-03 |
| Поиск, профиль | UNKNOWN (Q-11) | UNKNOWN | HOME-02, HOME-03 |

| Кредитная карта | Зарезервировано [D-05] | Зарезервировано | `CREDIT-xx` |
| Кешбэк | Зарезервировано [D-05] | Зарезервировано | `CASHBACK-xx` |
| Накопления | Зарезервировано [D-05] | Зарезервировано | `SAVE-xx` |

Зарезервированные разделы в схеме R38 отсутствуют. По [D-05] под них оставлено место и в меню, и в контракте, а проектируются они отдельно.

## Уровень 6 — граница адаптера (только граница)

Реализация адаптеров **вне рамок этой задачи**. Здесь зафиксировано только, где проходит граница.

| Адаптер | Что делает | Связь с ROADMAP |
|---|---|---|
| `DemoAdapter` | Отдаёт демо-данные из JSON-файлов. Изменения хранит так, как решим в вопросе 3 ROADMAP (в памяти или в хранилище браузера) | «JSON-адаптер» |
| `NullAdapter` | Банковского ядра нет: чтение возвращает пусто, любое действие — ответ «недоступно». Интерфейс показывает `StatusMessage(kind=unavailable)` | Новый. Доказывает, что UI работает без ядра |
| `FutureRealBankAdapter` | Подключение к настоящему API | «HTTP-адаптер». Вне scope |

**Правила границы:**

1. Интерфейс импортирует только **типы контракта**. Конкретный адаптер интерфейс не импортирует никогда.
2. Какой адаптер работает, выбирается **в одном месте** конфигурации.
3. Замена адаптера **не меняет** ни одного компонента и ни одной композиции экрана.
4. На каждый метод контракта у `NullAdapter` есть осмысленный ответ. Значит, каждому экрану нужно состояние «недоступно».
5. **Возможности устройства** (SIM, биометрия, камера) — это не банковское ядро. Как их изолировать, решим отдельно — [Q-21](#открытые-вопросы).

---

## Против раздувания

Это PET-проект. Всё перечисленное ниже **не вводится**, пока не появится явная необходимость:

- микросервисы, шина событий, Kubernetes;
- CQRS, распределённые транзакции;
- реальный KYC, интеграции с настоящими банками;
- отдельный доменный слой, фабрики адаптеров, внедрение зависимостей через контейнер;
- компоненты «на всякий случай»: `Checkbox`, `Avatar`, `RecipientSelector` отложены до появления экрана, где они нужны.

---

## Открытые вопросы

Вопросы Q-01…Q-17 описаны в [SCREEN_INVENTORY.md](SCREEN_INVENTORY.md#4-открытые-вопросы-по-инвентарю). Здесь — сводка по всем вопросам и новые архитектурные. Закрытые вопросы зачёркнуты, решение — в [журнале](#журнал-решений).

| № | Вопрос | Блокирует |
|---|---|---|
| Q-01 | 42 или 46 узлов в схеме | Сверку уровня 1 |
| Q-02 | Как выйти из ветки Open Account и как принимаются решения D1, D2 | Флоу A, F |
| Q-03 | MPIN — это пароль из ROADMAP? Сколько цифр? | `MPINInput` |
| ~~Q-04~~ | ~~Главный онбординг: R01 из ROADMAP или R38~~ | Решено — D-02 |
| ~~Q-05~~ | ~~SIM, биометрия и камера в браузере~~ | Решено — D-03 |
| ~~Q-06~~ | ~~Pay, UPI и UPI Functions — один раздел или разные~~ | Решено — D-04 |
| Q-07 | Порядок OTP и MPIN; OTP при смене MPIN | Флоу B, SET-02 |
| Q-08 | Вход без биометрии | AUTH-01 |
| ~~Q-09~~ | ~~Что показывается на главной, а что в меню~~ | Решено — D-04 |
| Q-10 | Какие узлы — состояния или варианты других экранов | Анализ дублирования |
| Q-11 | Содержимое 9 экранов (Search, Profile, счёт, выписка, Pay, оплата услуг, Функции СБП, отделения, уведомления) | Уровни 3–5 |
| Q-12 | Нужны ли кандидаты ACC-04…06 и SVC-01, SVC-02, SVC-04 | Уровень 1 |
| Q-13 | Выбор получателя из контактов (есть только в ROADMAP) | `RecipientSelector` |
| ~~Q-14~~ | ~~UPI или СБП или SPEI; формат реквизитов банка~~ | Решено — D-01 |
| Q-15 | Второй Card Services — дубль или другой экран | CARD-01 |
| Q-16 | Подтверждение выхода и куда вести после него | SET-01 |
| Q-17 | Языки интерфейса | SET-04 |
| **Q-18** | Нужен ли MPIN для подтверждения платежа (в UPI на схеме-источнике — да, в российских банках обычно нет) | PST-01, `PaymentConfirmation` |
| **Q-19** | Конфликт имени `ActionButton` с пакетом Alfa `action-button` | Именование в слое B |
| ~~Q-20~~ | ~~Кредитная карта, кешбэк и накопления из ROADMAP (этапы 5–7) — откуда брать экраны: другая карта флоу или проектируем сами~~ | Решено — D-05 |
| **Q-21** | Как изолировать возможности устройства (SIM, биометрия, камера): отдельная маленькая граница с демо-реализацией или внутри адаптера | Уровень 6 |
| **Q-22** | Онбординг по R38 [D-02]. Какие поля регистрации по Revolut (R01) из ROADMAP войдут в AUTH-02 и AUTH-05: телефон, адрес, почта, дата рождения, согласие на уведомления? Выбор страны при регионе «Россия» [D-01], скорее всего, не нужен | AUTH-02, AUTH-05, уровень 5 |
| ~~Q-23~~ | ~~Узлы UPI без прямого аналога в СБП~~ | Решено — D-06 |

---

## Проверка качества

| Проверка | Результат |
|---|---|
| У каждого экрана есть ID? | Да, 36 ID; узел N45 исключён решением D-06. У HOME-01 нет своего узла на схеме — отмечено. Зарезервированные разделы имеют префиксы, но не экраны |
| Дубли выявлены? | Да: N29 → CARD-01, N35 → PAY-01, N36 → PAY-03. Pay в меню и в нижней навигации — один экран [D-04] |
| Узлы флоу отделены от экранов? | Да: Start, D1, D2, N11, Logout, метки рёбер |
| Глобальные компоненты отделены от экранов? | Да: `BottomNavigation` (N19), `AppShell`, `AppHeader` |
| Семантические компоненты переиспользуются? | 17 из 24 — на двух и более экранах или в нескольких флоу (часть — с учётом предложений [A]). 6 используются на одном экране, это обосновано; `RecipientSelector` отложен. Одноэкранные описаны в [COMPONENT_INVENTORY.md](COMPONENT_INVENTORY.md#компоненты-которые-используются-на-одном-экране) |
| В примитивах нет бизнес-логики? | Да, они только обёртки над Alfa |
| Специфичных для экрана компонентов минимум? | Да, компонентов вроде `LoginButton` или `PaymentScreenHeader` нет |
| Кандидаты помечены? | Да, 7 CANDIDATE (из них SBP-02 — группа навигации), композиция для них не проектируется |
| Допущения записаны? | Да, метки `[A]` и `[R]` во всех документах; решения — метки `[D-xx]` |
| Один компонент используется в разных флоу? | Да: `ActionButton`, `OperationStatus`, `MPINInput`, `PaymentForm`, `ListRow` |
| Лишней сложности нет? | Уровни 5–6 заблокированы, доменный слой и DI не вводятся |
