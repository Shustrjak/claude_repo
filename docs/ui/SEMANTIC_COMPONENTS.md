# Семантические компоненты (Уровень 4 — Component Logic)

Часть UI-спецификации Banking Shell. Список компонентов и примитивов — в [COMPONENT_INVENTORY.md](COMPONENT_INVENTORY.md).

Входные данные описаны **концептуально**. Это не TypeScript-типы: точные типы появятся вместе с API-контрактом (уровень 5).

---

## Правила

1. **Компонент не знает, на каком он экране.** Например, `PaymentConfirmation` знает сумму, получателя, комиссию и статус, но не знает, что он «на PAY-04».
2. **Компоненты не ходят за данными.** Данные и действия приходят через входные параметры. Адаптер вызывает только экран — через хук фичи (см. [UI_ARCHITECTURE.md](UI_ARCHITECTURE.md#границы-логики)).
3. **Бизнес-логики нет ни в одном компоненте.** Бизнес-логика — это правила вроде «хватает ли денег», «лимит превышен», «MPIN верный». Её результат приходит в компонент готовым: статусом или ошибкой.
4. **UI-логика допустима.** Это таймер, маска ввода, проверка формата поля, доступ к камере — то, что не зависит от банка.
5. **Намерение вместо подписи.** Компонент получает смысл (`action="confirm"`), а текст и иконку выбирает сам.
6. **Никаких универсальных компонентов.** Если у компонента появляются десятки несвязанных параметров, его нужно разделить.

**Тип логики** в карточках:

| Тип | Что значит |
|---|---|
| Отображение | Только показывает то, что получил |
| UI-логика | Хранит своё UI-состояние: таймер, фокус, проверку формата |

---

## ActionButton

| Поле | Описание |
|---|---|
| Назначение | Основная кнопка действия. Текст и иконка берутся из намерения |
| Входные данные | `action`, `disabled`, `loading`; `label` — только если нужно переопределить стандартную подпись |
| Состояния | Обычное, неактивное, загрузка |
| Варианты | По `action`, см. таблицу ниже. Вид (primary, secondary, link) задан для каждого намерения |
| События | `onPress` |
| Где используется | Все флоу, 13 экранов и состояний (матрица — в [COMPONENT_INVENTORY.md](COMPONENT_INVENTORY.md#матрица-переиспользования)) |
| Зависит от | `Button`, `Icon` |
| Тип логики | Отображение |

Таблица намерений. Подписи и иконки — предложение [A].

| `action` | Подпись | Иконка | Вид |
|---|---|---|---|
| `next` | Далее | Стрелка вправо | primary |
| `confirm` | Подтвердить | Галочка | primary |
| `pay` | Оплатить | — | primary |
| `retry` | Повторить | Круговая стрелка | secondary |
| `cancel` | Отмена | — | secondary |
| `close` | Готово | Галочка | primary |

- **Чего нет:** намерений под конкретный экран (`register`, `login`) и `logout`. Выход — это строка `ListRow(danger)` в настройках.
- **Как расширять:** новое намерение добавляется, только если оно встречается хотя бы в двух флоу.

## AuthMethodSelector

| Поле | Описание |
|---|---|
| Назначение | Выбор способа подтверждения личности: биометрия или MPIN. Узел N04 и подтверждение платежа [D-13] |
| Входные данные | `methods` — доступные способы; `current` — выбранный; `biometricAvailable` — доступна ли биометрия (экран узнаёт это у границы устройства [D-14]) |
| Состояния | Идёт биометрия, введите MPIN, биометрия недоступна |
| Варианты | — |
| События | `onSelect(method)`, `onBiometricRequest` |
| Где используется | AUTH-01, PST-01 [D-13] |
| Зависит от | `IconButton`, `Typography`, `MPINInput` |
| Тип логики | UI-логика: переключение способа. Способы — ИЛИ, не подряд [D-13]. Биометрию проверяет граница устройства по событию, MPIN проверяет адаптер; компонент сам ничего не проверяет |

## MPINInput

| Поле | Описание |
|---|---|
| Назначение | Ввод MPIN с цифровой клавиатурой |
| Входные данные | `mode`: `enter` (вход или подтверждение), `create` (новый), `confirm` (повтор); `error`. Длина — ровно 4 цифры [D-09], не настраивается |
| Состояния | Пусто, частично введён, введён полностью, ошибка |
| Варианты | По `mode`: меняется только заголовок |
| События | `onComplete(pin)` — после 4-й цифры |
| Где используется | AUTH-01, AUTH-06, SET-02, PST-01 |
| Зависит от | `PassCode` |
| Тип логики | UI-логика: ввод и маска. Совпадение двух MPIN проверяет форма экрана. Верный ли MPIN — решает адаптер |

## OTPVerification

| Поле | Описание |
|---|---|
| Назначение | Подтверждение одноразовым кодом |
| Входные данные | `destination` — куда отправлен код, например маска телефона; `codeLength`; `resendAfter` — секунды до повторной отправки; `status`; `error` |
| Состояния | Ввод, проверка, ошибка [S], успех [S], можно отправить ещё раз |
| Варианты | — |
| События | `onSubmit(code)`, `onResend` |
| Где используется | AUTH-07 (онбординг), SET-02 (чувствительная операция) [D-10]. Назначение OTP компонент не знает: это параметр запроса в контракте |
| Зависит от | `CodeInput`, `Typography`, `ActionButton(retry)` |
| Тип логики | UI-логика: таймер повтора. Верный ли код — решает адаптер |

## ChoiceList

| Поле | Описание |
|---|---|
| Назначение | Выбор одного варианта из короткого списка |
| Входные данные | `items`: `{ id, title, description?, icon? }`; `value` |
| Состояния | Ничего не выбрано, выбран вариант, вариант недоступен |
| Варианты | — |
| События | `onChange(id)` |
| Где используется | AUTH-03 (SIM), SET-04 (язык) |
| Зависит от | `Radio`, `Cell` |
| Тип логики | Отображение. Что такое SIM и язык, компонент не знает |

## OperationStatus

| Поле | Описание |
|---|---|
| Назначение | Показать ход и итог операции |
| Входные данные | `status`: `pending`, `success`, `failure`; `title`; `description`; блок действий (слот) |
| Состояния | Три статуса |
| Варианты | Во весь экран или внутри экрана |
| События | Нет своих: действия приходят через слот `ActionButton` |
| Где используется | PST-02 (результат платежа), SET-02 (успех смены MPIN) [D-10]; AUTH-04 (привязка SIM), SBP-03 [A] |
| Зависит от | `Spinner`, `Icon`, `Typography`, `StatusBadge` |
| Тип логики | Отображение |

## AccountCard

| Поле | Описание |
|---|---|
| Назначение | Краткая информация о счёте |
| Входные данные | `name`, `maskedNumber`, `balance` (сумма и валюта), `status` |
| Состояния | Обычное, счёт заблокирован [A] |
| Варианты | Компактный, подробный [A] |
| События | `onPress` |
| Где используется | HOME-01 [A]. ACC-01 — SOURCE_REQUIRED (Q-11) |
| Зависит от | `Money`, `Typography`, `StatusBadge` |
| Тип логики | Отображение |

## TransactionRow

| Поле | Описание |
|---|---|
| Назначение | Одна операция в списке |
| Входные данные | `title`, `date`, `amount` (со знаком), `status`, `category` [A] |
| Состояния | Проведена, в обработке, отклонена [A] |
| Варианты | — |
| События | `onPress` [A] |
| Где используется | Внутри `TransactionList` |
| Зависит от | `Cell`, `Money`, `StatusBadge` |
| Тип логики | Отображение |

## TransactionList

| Поле | Описание |
|---|---|
| Назначение | Список операций |
| Входные данные | `items`, `variant`, `limit` |
| Состояния | Пусто (через `StatusMessage`), есть операции |
| Варианты | `compact` — последние N операций для мини-выписки; `full` — для подробной выписки [A], ждёт источника: ACC-03 — SOURCE_REQUIRED (Q-11) |
| События | `onItemPress` [A] |
| Где используется | ACC-02 (`compact`); HOME-01 (`compact`) [A], см. [Q-10](SCREEN_INVENTORY.md#q-10) |
| Зависит от | `TransactionRow`, `StatusMessage` |
| Тип логики | Отображение. Группировка по датам — UI-логика |

## BankCard

| Поле | Описание |
|---|---|
| Назначение | Изображение банковской карты |
| Входные данные | `maskedNumber`, `holder` [A], `expiry` [A], `status`, `kind` (физическая или виртуальная [R]) |
| Состояния | Активна, заморожена [R], заблокирована [A] |
| Варианты | UNKNOWN до уточнения CARD-01 |
| События | `onPress` |
| Где используется | CARD-01 [A] |
| Зависит от | `bank-card` или `card-image` из Alfa, `StatusBadge` |
| Тип логики | Отображение |

## PaymentMethodSelector

| Поле | Описание |
|---|---|
| Назначение | Выбор способа оплаты |
| Входные данные | `methods` — подмножество из `qr` (QR СБП), `mobile` (по телефону через СБП), `bankAccount` (по реквизитам, остаётся в СБП) [S — ветка UPI на схеме; D-01, D-06] |
| Состояния | Способ недоступен [A] |
| Варианты | Список или плитки [A] |
| События | `onSelect(method)` |
| Где используется | SBP-01. Содержимое PAY-01 — SOURCE_REQUIRED (Q-11) |
| Зависит от | `ListRow` или `ServiceTile` |
| Тип логики | Отображение. Куда вести после выбора, решает экран |

## RecipientInput

| Поле | Описание |
|---|---|
| Назначение | Ввод получателя |
| Входные данные | `type`: `mobile`, `bankAccount`; `value`; `error`; `resolvedName` — имя получателя [A]; `banks` — банки получателя для `mobile` [D-06]. Оба значения передаёт экран: он получает их от адаптера, компонент сам никуда не обращается |
| Состояния | Пусто, ввод, формат неверный, получатель найден, не найден; для `mobile` — банки загружаются, банков нет [A] |
| Варианты | По `type`: `mobile` — телефон (+7) и выбор банка получателя [D-06]; `bankAccount` — номер счёта и БИК [D-01] |
| События | `onChange(value)`; для `mobile` — `onBankChange(bankId)` |
| Где используется | PAY-04, PAY-06 |
| Зависит от | `PhoneInput`, `Input`, `Select` |
| Тип логики | UI-логика: проверка формата. Существует ли получатель — решает адаптер |

## RecipientSelector (не входит в v1)

| Поле | Описание |
|---|---|
| Назначение | Выбор получателя из контактов и недавних |
| Источник | Только ROADMAP, этап 4 (R01) [R]. На схеме R38 этого нет |
| Статус | Не входит в v1 [D-12]: ни один активный флоу его не требует, доступ к контактам и список недавних не нужны |

## PaymentForm

| Поле | Описание |
|---|---|
| Назначение | Форма платежа: получатель, сумма, комментарий |
| Входные данные | `recipientType`, `initialRecipient` (например, из QR), `currency`, `limits` (минимум и максимум, приходят от адаптера) |
| Состояния | Заполняется, есть ошибки, готова к отправке |
| Варианты | По `recipientType`. Сама форма одна и та же |
| События | `onSubmit({ recipient, amount, comment })` |
| Где используется | PAY-04, PAY-06; PAY-03 [A] |
| Зависит от | `RecipientInput`, `AmountInput`, `Input`, `ActionButton(next)` |
| Тип логики | UI-логика: проверка формы (Zod). Хватит ли денег и можно ли платить — решает адаптер на шаге подтверждения |

## PaymentSummary

| Поле | Описание |
|---|---|
| Назначение | Сводка платежа |
| Входные данные | `amount`, `recipient`, `fee` [A], `total` [A], `comment` |
| Состояния | — |
| Варианты | Краткая, подробная [A] |
| События | — |
| Где используется | PST-01, PST-02; PAY-04, PAY-06 [A] |
| Зависит от | `Money`, `Typography`, `Divider` |
| Тип логики | Отображение. Комиссию не считает, получает готовой |

## PaymentConfirmation

| Поле | Описание |
|---|---|
| Назначение | Подтверждение платежа перед отправкой (PST-01) |
| Входные данные | `summary` (данные для `PaymentSummary`); `methods` — биометрия и MPIN; `biometricAvailable`; `status`: `idle`, `submitting`, `error` |
| Состояния | Ожидает подтверждения, отправляется, ошибка |
| Варианты | Шторка или экран [A] |
| События | `onConfirm({ method, pin? })`, `onBiometricRequest`, `onCancel` |
| Где используется | PST-01 — общий шаг для флоу PAY-04, PAY-06; PAY-03 [A] |
| Зависит от | `PaymentSummary`, `AuthMethodSelector`, `MPINInput`, `ActionButton(pay)`, `ActionButton(cancel)` |
| Тип логики | Отображение. Способ — биометрия ИЛИ MPIN, не оба [D-13]; MPIN — запасной способ. Биометрию проверяет граница устройства [D-14], MPIN и сам платёж — адаптер. Компонент не знает, с какого экрана пришёл платёж |

## QRScanner

| Поле | Описание |
|---|---|
| Назначение | Сканирование QR-кода камерой |
| Входные данные | — |
| Состояния | Запрос доступа, камера запрещена, сканирование, QR не распознан, распознан [A] |
| Варианты | — |
| События | `onScan(payload)`, `onError(reason)` |
| Где используется | PAY-03 (открывается из СБП и из нижней навигации) |
| Зависит от | Камера — через границу устройства [D-14], экран передаёт доступ; библиотека распознавания QR (выбрать позже); `StatusMessage` |
| Тип логики | UI-логика: работа с камерой. Что внутри QR, разбирает экран или адаптер |

## NotificationItem

| Поле | Описание |
|---|---|
| Назначение | Одно уведомление |
| Входные данные | UNKNOWN — содержимое NOTIF-01 не описано, см. [Q-11](SCREEN_INVENTORY.md#q-11) |
| Где используется | NOTIF-01 — SOURCE_REQUIRED |
| Статус | Проектируется после появления источника |

## ServiceTile

| Поле | Описание |
|---|---|
| Назначение | Плитка раздела или услуги |
| Входные данные | `title`, `icon`, `badge` [A], `disabled` |
| Состояния | Обычная, недоступна (отключённый пункт [D-11] или NullAdapter) |
| Варианты | — |
| События | `onPress` |
| Где используется | HOME-04 [A] — альтернатива `ListRow` |
| Зависит от | `Icon`, `Typography`, `Badge` |
| Тип логики | Отображение |

## ListRow

| Поле | Описание |
|---|---|
| Назначение | Строка списка |
| Входные данные | `title`, `subtitle`, `icon`, `value` (для `value`), `checked` (для `toggle`), `copyable` [R] |
| Состояния | Обычная, неактивная (в том числе отключённый пункт навигации [D-11]) |
| Варианты | `link` — со стрелкой, ведёт дальше; `toggle` — с переключателем; `value` — показывает значение; `danger` — опасное действие |
| События | `onPress`, `onToggle(checked)`, `onCopy` [R] |
| Где используется | AUTH-01, AUTH-06, HOME-04, ACC-07, SET-01, SET-03; HOME-01, CARD-01, SBP-03 [A] |
| Зависит от | `Cell`, `Switch`, `Icon` |
| Тип логики | Отображение |

## SearchResult

| Поле | Описание |
|---|---|
| Назначение | Одна строка результата поиска |
| Входные данные | UNKNOWN — зависит от того, по чему ищем, см. [Q-11](SCREEN_INVENTORY.md#q-11) |
| Где используется | HOME-02 — SOURCE_REQUIRED |
| Статус | Проектируется после появления источника |

## StatusMessage

| Поле | Описание |
|---|---|
| Назначение | Сообщение вместо содержимого |
| Входные данные | `kind`: `empty`, `error`, `unavailable`; `title`; `description`; действие (слот) |
| Состояния | По `kind` |
| Варианты | `unavailable` — «банковское ядро не подключено», нужен для NullAdapter |
| События | Через слот `ActionButton(retry)` |
| Где используется | Все экраны с данными |
| Зависит от | `system-message` из Alfa, `Icon`, `Typography` |
| Тип логики | Отображение |

## AsyncContent

| Поле | Описание |
|---|---|
| Назначение | Выбрать, что показать: загрузку, ошибку, пустоту или данные |
| Входные данные | Состояние запроса (`loading`, `error`, `empty`, `data`), каркас загрузки (`skeleton`), содержимое |
| Состояния | Четыре перечисленных |
| Варианты | — |
| События | `onRetry` |
| Где используется | Все экраны, которые загружают данные |
| Зависит от | `Skeleton`, `StatusMessage` |
| Тип логики | Отображение. Сам данные не загружает |

## StatusBadge

| Поле | Описание |
|---|---|
| Назначение | Статус операции, карты или счёта |
| Входные данные | `status` — нейтральный: `success`, `pending`, `failure`, `blocked`, `frozen`; `label` |
| Где используется | Внутри `TransactionRow`, `BankCard`, `AccountCard`, `OperationStatus` |
| Зависит от | `status-badge` или `status` из Alfa |
| Тип логики | Отображение |

---

## Глобальные компоненты

| Компонент | Входные данные | Логика |
|---|---|---|
| `AppShell` | `variant`: `auth` (без нижней навигации) или `main` (с ней) [A]; содержимое | Отображение |
| `AppHeader` | `title`, `back` (показывать ли кнопку «назад»), `actions` — иконки справа | Отображение. Куда вести «назад», решает роутер, а не шапка |
| `BottomNavigation` | `items` — Home, Pay, Scan QR, Settings, Функции СБП [S, D-01]; `active` | Отображение. Активный пункт определяется по текущему маршруту |
